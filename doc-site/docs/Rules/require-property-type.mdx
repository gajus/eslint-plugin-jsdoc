# `require-property-type`

## Introduction

Requires that each `@property` tag has a `type` value (within curly brackets).

## Context and settings

|||
|---|---|
|Context|everywhere|
|Tags|`property`|
|Aliases|`prop`|
|Recommended|true|

import Assertions from '@site/src/components/Assertions.js';

## Failed assertions

<Assertions rule="requirePropertyType" />

## Passing assertions

<Assertions rule="requirePropertyType" passing />
